Ecrire une classe User          ( GitHub marak/faker ) -> Librairie 
- First Name                         
- Last Name                      
- eMail
- numTel
- age

Ecrire classe employee extends User
- Job Title
- Salaire
- Génerer un tableau de 100 employées 
- Ecrire une méthode =>
    - age moyen 
    - Salaire moyen 



Aide de code =>          (min, max) => Math.floor(Math.random()*(max-min+1))+min;


pour utiliser faker = > faker.name.lastName()
                        faker.name.firstName()
                        faker.internet.email()


Cours:

Exemple d'utilisation map:

arg = arg. morph(arg => arg * 2 );

Ancien code vs nouveau : function(arg) {     ->      arg => { arg * 2 
                            return arg *2               return arg   
                                                        }                        
[1,2,3].map((val)=> {
    return *2;
};
[ u,v,w].map(user => user.salary);


Exemple d'utilisation reduce:

[1,2,3]reduce ((acc, tab) => {      ou bien comme ça->       [1,2,3].reduce((acc,tab)=> acc + tab, 0);
    return acc + tab;


};
                       
                        
