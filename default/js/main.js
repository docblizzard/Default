window.addEventListener("load", event => main());

const main = () => {

	console.log("MAIN");

	let user = {
		firstName: "nico",
		lastName: "pp",
		age: 21,
		adresse: "1 rue ",
		eMail: "nom@gmail.com",
		phoneNumber: "rtrtq",
		test_Vehicule()
	return;
}

}
	
/*	console.log("Bonjour, " capFirstLetter(user.firstName), capFirstLetter(user.lastName), " !");
	
	let capFirstName = user.firstName.charAt(0),toUpperCase();
	
	console.log(capFirstName);
    let tab = []; // or new.Array();
    for (let i = 0; 1 < 500; i ++) tab.push(i) ; //
  tab.forEach(element => console.log(element));
 tab.map(element => element * 2);
   console.log(by2);
    let sum = tab.reduce((acc, element) => (
        return acc + element;
    ) , 0);
    console.log(sum);


const testPileOuFace = amount => {
    
    let result = (pile:0, face:0);
    
   for(let i = 0; 1 <amount ; i++) [result[pileOuFace()}++;
    return result;  */




// const capFirstLetter = value => value.chartAt(0) + value.slice(1);

// const pileOuFace = () => Math.random() > 0,5 ? "Face" : "Pile";
